<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Chat App</title>
  <script>
    const roomName = "<%= roomName %>"
  </script>
  <script defer src="http://kisko.xyz:3113/socket.io/socket.io.js"></script>
  <script defer src="script.js"></script>
  <style>
    body,html{
      height: 100%;
      margin: 0;
    }
    body{
      margin: 0;
      width: 100%;
      height: 100%;
      background-size: 100% 100%;
      background-repeat: no-repeat;
      position:absolute;
      display: flex;
      justify-content: center;
	  font-family:sans-serif;
    }
    #vid1{
      top: -30px;
      left:0 ;
      position: absolute;
    }
    #message-container {
      position: absolute;
      width: 90%;
      max-width: 1200px;
    }

    #message-container div {
      background-color: #eeeeee;
	  box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.125);
      padding: 5px;
	  margin:10px;
    }
    #send-container {
      position: fixed;
      bottom: 10%;
    }
    #message-input {
      width: 89%;
    }
  </style>
</head>
<body>
<div id="v1">
  <video id="vid1" autoplay width=210 height="220"></video>
  <script>
    let constraints1 = { audio: true, video: { width: 1280, height: 720 } };

    navigator.mediaDevices.getUserMedia(constraints1)
            .then(function(mediaStream) {
              let video = document.querySelector('video');
              video.srcObject = mediaStream;
              video.onloadedmetadata = function(e) {
                video.play();
              };
            })
            .catch(function(err) { console.log(err.name + ": " + err.message); });
  </script>
</div>
<br/>
  <div id="message-container"></div>
  <form id="send-container">
    <input type="text" id="message-input">
    <button type="submit" id="send-button"><strong>Send</strong></button>
  </form>
</body>
</html>